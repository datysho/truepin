// TruePin - deploy configuration (no build step, so this is a plain file).
// TP_EXTPAY_ID - this extension's id on extensionpay.com. Empty string =
// the voluntary-support layer is fully dormant: no Support section in
// options, no extpay.js in the service worker, no network calls.
const TP_EXTPAY_ID = "";
